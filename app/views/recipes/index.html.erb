<h1 class="title is-1">Very Fancy recipies!</h1>
<ul>
  <% @recipes.each do | recipe | %>
    <li> <%= link_to recipe.title, recipe %> in the <%= recipe.category.name %> category.</li>
  <% end %>
</ul>
